@* @model IEnumerable<MaquinaViewModel>

@{
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
    ViewBag.Title = "Máquinas";
}

<!-- ============================================================== -->
<!-- Bread crumb and right sidebar toggle -->
<!-- ============================================================== -->
<div class="page-breadcrumb">
    <div class="row">
        <div class="col-5 align-self-center">
            <h4 class="page-title">Lista de máquinas</h4>
        </div>
        <div class="col-7 align-self-center">
            <div class="d-flex align-items-center justify-content-end">
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item">
                            <a href="@Url.Action("Administracion", "Home")">Administración</a>
                        </li>
                        <li class="breadcrumb-item active" aria-current="page">
                            Máquinas
                        </li>
                    </ol>
                </nav>
            </div>
        </div>
    </div>
</div>
<!-- ============================================================== -->
<!-- End Bread crumb and right sidebar toggle -->
<!-- ============================================================== -->
<!-- ============================================================== -->
<!-- Container fluid  -->
<!-- ============================================================== -->
<div class="container-fluid">
    <!-- ============================================================== -->
    <!-- Start Page Content -->
    <!-- ============================================================== -->
    <div class="row">
        <div class="col-12">
            <div class="card shadow-sm">
                <div class="card-body">
                    <a asp-action="Nuevo" class="btn btn-sm btn-default">Agregar máquina</a>

                    <hr />

                    @foreach (var Maquina in Model)
                    {
                        <div class="card border" style="width: 18rem;">
                            <div class="img-thumbnail-sm">
                                <a data-fancybox href="@Maquina.URLImagen.AbsolutePath">
                                    <img src="@Maquina.URLImagen.AbsolutePath" class="img-top img-fluid"
                                     alt="Imagen de la máquina" />
                                </a>
                            </div>
                            <div class="card-body">
                                <h5 class="card-title text-center mb-0">
                                    @Maquina.Nombre
                                </h5>
                                <div class="card-body">
                                    <p class="">
                                        @Maquina.NombreTipoMaquina
                                        <br />
                                        @Maquina.Codigo
                                        <br />
                                        @Maquina.NumeroActivo
                                        <br />
                                        @Maquina.FechaAdquisicion.ToString("MM/dd/yyyy")
                                    </p>
                                    <small class="text-info">@Maquina.Estado</small>
                                </div>
                            </div>
                            <div class="card-footer">
                                <button id="BtnDetalle" class="btn btn-sm btn-success" data-toggle="modal"
                                    data-target="#ModalDetalle"
                                    data-idmaquina="@Maquina.Id">
                                    Detalle
                                </button>

                                <div class="btn-group btn-group-sm" role="group" aria-label="Basic example">
                                    <a class="btn btn-sm btn-secondary" asp-controller="Maquinas"
                                   asp-action="Editar" asp-route-id="@Maquina.Id">Actualizar</a>
                                    <a class="btn btn-sm btn-danger" asp-controller="Maquinas"
                                   asp-action="Eliminar" asp-route-id="@Maquina.Id">Eliminar</a>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
    <!-- ============================================================== -->
    <!-- End Page Content -->
    <!-- ============================================================== -->
</div>
<!-- ============================================================== -->
<!-- End Container fluid  -->
<!-- ============================================================== -->

<partial name="_Detalle" />

<link rel="stylesheet" href="~/lib/fancybox/jquery.fancybox.min.css" />

@section Scripts{
    <script type="text/javascript" src="~/lib/fancybox/jquery.fancybox.min.js"></script>

    <script type="text/javascript">
        $('#ModalDetalle').on('show.bs.modal', function (event) {
            var button = $(event.relatedTarget); // Button that triggered the modal
            var IdMaquina = button.data('idmaquina'); // Extract info from data-* attributes

            $("#ModalDetalle").find('.modal-title .nombre').text("Cargando...");

            // Initiate an AJAX request here(and then do the updating in a callback).
            $.ajax({
                url: '@Url.Action("Detalle")',
                data: { Id: IdMaquina },
                dataType: "json",
                type: 'GET',
                success: handleData,
                error: handleError
            });
        });

        function handleData(data) {
            // Update the modal's content. We'll use jQuery here, but you could use a data binding library or other methods instead.
            var modal = $("#ModalDetalle");
            modal.find('.modal-title .nombre').text(data.nombre);
            modal.find('.modal-body .codigo').text(data.codigo);
            modal.find('.modal-body .numero-activo').text(data.numeroActivo);
            modal.find('.modal-body .tipo-maquina').text(data.nombreTipoMaquina);
            modal.find('.modal-body .estado').text(data.estado);
            modal.find('.modal-body .fecha-adquisicion').text(data.fechaAdquisicion);
            modal.find('.modal-body .grupos-musculares').text(data.gruposMusculares.map(obj => obj.nombre).join(", "));

        }

        function handleError() {
            // Update the modal's content. We'll use jQuery here, but you could use a data binding library or other methods instead.
            var modal = $("#ModalDetalle");
            modal.find('.modal-title .nombre').text("Error al obtener la información");
            modal.find('.modal-body .tipo-maquina').text('N/A');
            modal.find('.modal-body .estado').text('N/A');
            modal.find('.modal-body .grupos-musculares').text('N/A');
        }
    </script>
} *@
