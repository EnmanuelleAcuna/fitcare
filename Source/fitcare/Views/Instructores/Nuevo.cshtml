@* @model NuevoInstructorViewModel

@{
Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
ViewBag.Title = "Instructores";
}

<!-- ============================================================== -->
<!-- Bread crumb -->
<!-- ============================================================== -->
<div class="page-breadcrumb">
<div class="row">
<div class="col-5 align-self-center">
<h4 class="page-title">Agregar instructor</h4>
</div>
<div class="col-7 align-self-center">
<div class="d-flex align-items-center justify-content-end">
<nav aria-label="breadcrumb">
<ol class="breadcrumb">
<li class="breadcrumb-item">
<a href="@Url.Action("Administracion", "Home")">Administración</a>
</li>
<li class="breadcrumb-item">
<a href="@Url.Action("Inicio")">Instructores</a>
</li>
<li class="breadcrumb-item active" aria-current="page">
Agregar
</li>
</ol>
</nav>
</div>
</div>
</div>
</div>
<!-- ============================================================== -->
<!-- End Bread crumb -->
<!-- ============================================================== -->
<!-- ============================================================== -->
<!-- Container fluid -->
<!-- ============================================================== -->
<div class="container-fluid">
<!-- ============================================================== -->
<!-- Start Page Content -->
<!-- ============================================================== -->
<div class="row">
<div class="col-12">
<div class="card shadow-sm">
<div class="card-body">
@using (Html.BeginForm("Nuevo", "Instructores", FormMethod.Post, new { autocomplete = "off", enctype = "multipart/form-data" })) {
@Html.AntiForgeryToken()

<div class="row">
<div class="col-md-12">
@Html.ValidationSummary(true, "")
</div>
</div>

<div class="row">
<div class="col-sm-12 col-md-4">
<div class="form-group">
@Html.LabelFor(model => model.IdUsuario)
@Html.DropDownListFor(model => model.IdUsuario, new List<SelectListItem>(ViewBag.ListaUsuarios), "Seleccione un usuario", new { @class = "form-control
form-control-sm" })
@Html.ValidationMessageFor(model => model.IdUsuario)
</div>
</div>

<div class="col-sm-12 col-md-4">
<div class="form-group">
@Html.LabelFor(model => model.FechaIngreso)
<br />
@Html.EditorFor(model => model.FechaIngreso, "", null, new { @class = "form-control form-control-sm", placeholder = "Digite la fecha de ingreso" })
@Html.ValidationMessageFor(model => model.FechaIngreso, "", new { @class = "text-danger" })
</div>
</div>

<div class="col-sm-12 col-md-4">
<div class="form-group">
@Html.LabelFor(model => model.Fotografia)
@Html.TextBoxFor(model => model.Fotografia, new { @class = "form-control form-control-sm", type = "file" })
@Html.ValidationMessageFor(model => model.Fotografia, "", new { @class = "text-danger d-block" })
</div>
</div>

<div class="col-sm-12 col-md-4">
<div class="form-group">
@Html.Label("Provincia")
@Html.DropDownList("Provincias", new List<SelectListItem>(ViewBag.ListaProvincias), "Seleccione una provincia", new { @class = "form-control form-control-sm" })
</div>
</div>

<div class="col-sm-12 col-md-4">
<div class="form-group">
@Html.Label("Canton")
@Html.DropDownList("Cantones", new List<SelectListItem>(ViewBag.ListaCantones), "Seleccione un canton", new { @class = "form-control form-control-sm" })
</div>
</div>

<div class="col-sm-12 col-md-4">
<div class="form-group">
@Html.LabelFor(model => model.IdDistrito)
@Html.DropDownListFor(model => model.IdDistrito, new List<SelectListItem>(ViewBag.ListaDistritos), "Seleccione un distrito", new { @class = "form-control
form-control-sm" })
@Html.ValidationMessageFor(model => model.IdDistrito)
</div>
</div>

<div class="col-12">
<input value="Guardar" class="btn btn-sm btn-default" type="submit" />
@Html.ActionLink("Regresar", "Inicio", null, null, new { @class = "btn btn-sm btn-secondary" })
</div>
</div>
}
</div>
</div>
</div>
</div>
<!-- ============================================================== -->
<!-- End Page Content -->
<!-- ============================================================== -->
</div>
<!-- ============================================================== -->
<!-- End Container fluid -->
<!-- ============================================================== -->

@section Scripts{
@{await Html.RenderPartialAsync("_ValidationScriptsPartial");}

<script type="text/javascript">
$(document).ready(function () {
const cantones = @Json.Serialize(@ViewBag.ListaCantones);
const distritos = @Json.Serialize(@ViewBag.ListaDistritos);

$('#Provincias').on('change', function() {
const selectedValue = $(this).val();

// Filter the array based on the value of the "age" property
const filteredCantones = cantones.filter((element) => {
return element.idPadre == selectedValue;
});

const cantonesSelect = $('#Cantones');

LimpiarSelectCantones(cantonesSelect);

filteredCantones.forEach((element) => {
const optionElement = $('<option>').attr('value', element.value).text(element.text);
cantonesSelect.append(optionElement);
});

LimpiarSelectDistritos($('#IdDistrito'));
});

$('#Cantones').on('change', function() {
const selectedValue = $(this).val();

const filteredDistritos = distritos.filter((element) => {
return element.idPadre == selectedValue;
});

const distritosSelect = $('#IdDistrito');

LimpiarSelectDistritos(distritosSelect);

filteredDistritos.forEach((element) => {
const optionElement = $('<option>').attr('value', element.value).text(element.text);
distritosSelect.append(optionElement);
});
});

function LimpiarSelectCantones(cantonesSelect) {
cantonesSelect.empty();
cantonesSelect.prepend($('<option>').attr('value', '').text('Seleccione un Canton'));
}

function LimpiarSelectDistritos(distritosSelect) {
distritosSelect.empty();
distritosSelect.prepend($('<option>').attr('value', '').text('Seleccione un Distrito'));
}
});
</script>
} *@
