@model IEnumerable<DistritoViewModel>

@{
	ViewBag.Title = "Distritos";
}

<div class="container">
	<div class="row">
		<div class="col-5 align-self-center">
			<h6 class="page-title">Lista de distritos</h6>
		</div>
		<div class="col-7 align-self-center">
			<div class="d-flex align-items-center justify-content-end">
				<nav aria-label="breadcrumb">
					<ol class="breadcrumb">
						<li class="breadcrumb-item">
							<a href="@Url.Action("Administracion", "Home")">Administración</a>
						</li>
						<li class="breadcrumb-item active" aria-current="page">
							Distritos
						</li>
					</ol>
				</nav>
			</div>
		</div>
	</div>
</div>

<div class="container-fluid">
	<div class="row">
		<div class="col-12">
			<div class="card shadow-sm">
				<div class="card-body">
					<a asp-action="AgregarDistrito" class="btn btn-sm btn-secondary">Agregar distrito</a>

					<hr />

					<div class="table-responsive">
						<table class="table table-hover table-sm text-nowrap">
							<thead>
								<tr>
									<th nowrap>@Html.DisplayNameFor(m => m.FirstOrDefault().Nombre)</th>
									<th nowrap>@Html.DisplayNameFor(m => m.FirstOrDefault().Canton)</th>
									<th nowrap>@Html.DisplayNameFor(m => m.FirstOrDefault().Estado)</th>
									<th nowrap>@Html.DisplayNameFor(m => m.FirstOrDefault().IdINEC)</th>
									<th></th>
								</tr>
							</thead>
							<tbody>
								@foreach (var Distrito in Model)
								{
									<tr>
										<td>@Distrito.Nombre</td>
										<td>@Distrito.Canton.Nombre</td>
										<td>@Distrito.Estado</td>
										<td>@Distrito.IdINEC</td>
										<td class="text-right">
											<div class="btn-group btn-group-sm" role="group" aria-label="Basic example">
												<a class="btn btn-sm btn-light" asp-controller="DivisionTerritorial" asp-action="EditarDistrito"
													asp-route-id="@Distrito.Id">Modificar</a>
												<a class="btn btn-sm btn-light text-danger" asp-controller="DivisionTerritorial" asp-action="EliminarDistrito"
													asp-route-id="@Distrito.Id">Eliminar</a>
											</div>
										</td>
									</tr>
								}
							</tbody>
						</table>
					</div>
				</div>
			</div>
		</div>
	</div>
</div>
