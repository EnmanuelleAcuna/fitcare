@model IEnumerable<CantonViewModel>

@{
	Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
	ViewBag.Title = "Cantones";
}

<div class="page-breadcrumb">
	<div class="row">
		<div class="col-5 align-self-center">
			<h4 class="page-title">Lista de cantones</h4>
		</div>
		<div class="col-7 align-self-center">
			<div class="d-flex align-items-center justify-content-end">
				<nav aria-label="breadcrumb">
					<ol class="breadcrumb">
						<li class="breadcrumb-item">
							<a href="@Url.Action("Administracion", "Home")">Administración</a>
						</li>
						<li class="breadcrumb-item active" aria-current="page">
							Cantones
						</li>
					</ol>
				</nav>
			</div>
		</div>
	</div>
</div>

<div class="container-fluid">
	<div class="row">
		<div class="col-12">
			<div class="card shadow-sm">
				<div class="card-body">
					<a asp-action="AgregarCanton" class="btn btn-sm btn-default">Agregar canton</a>

					<hr />

					<div class="table-responsive">
						<table class="table table-hover table-sm">
							<thead>
								<tr>
									<th nowrap>@Html.DisplayNameFor(m => m.FirstOrDefault().Nombre)</th>
									<th nowrap>@Html.DisplayNameFor(m => m.FirstOrDefault().Provincia)</th>
									<th nowrap>@Html.DisplayNameFor(m => m.FirstOrDefault().IdINEC)</th>
									<th nowrap>@Html.DisplayNameFor(m => m.FirstOrDefault().Estado)</th>
									<th>Acciones</th>
								</tr>
							</thead>
							<tbody>
								@foreach (var Canton in Model)
								{
									<tr>
										<td>@Canton.Nombre</td>
										<td>@Canton.Provincia</td>
										<td>@Canton.IdINEC</td>
										<td>@Canton.Estado</td>
										<td>
											<div class="btn-group btn-group-sm" role="group" aria-label="Basic example">
												<a class="btn btn-sm btn-secondary" asp-controller="Cantones" asp-action="Editar"
													asp-route-id="@Canton.Id">Modificar</a>
												<a class="btn btn-sm btn-danger" asp-controller="Cantones" asp-action="Eliminar"
													asp-route-id="@Canton.Id">Eliminar</a>
											</div>
										</td>
									</tr>
								}
							</tbody>
						</table>
					</div>
				</div>
			</div>
		</div>
	</div>
</div>
